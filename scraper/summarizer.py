# Optional LLM summarizer (not wired by default).

# If you later want AI-written summaries at scrape time, you can import
# this module from scraper.py and call batch_summarize(items) after parsing.
# Requires an API key and installing an SDK.

def batch_summarize(items):
    # Placeholder: return items unchanged
    return items
